<UserControl x:Class="NoNiDev.ArchipelagoParser.Views.RandoStatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:draw="clr-namespace:System.Drawing;assembly=System.Drawing"
                xmlns:conv="clr-namespace:NoNiDev.ArchipelagoParser.Converter"
             xmlns:local="clr-namespace:NoNiDev.ArchipelagoParser.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <conv:IconToImageSourceConverter x:Key="IconToImageSource"/>
        <conv:BooleanToVisibilityConverter x:Key="BooleanToVisibility"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!-- 0 -->
        <local:APIConnect Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                                      DataContext="{Binding RandoStatAPIVM}"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
            
            <ListBox ItemsSource="{Binding Players}" HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <local:RandoSlotView HorizontalContentAlignment="Stretch" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>



        <!--<Button Grid.Row="2" Grid.Column="0" Command="{Binding ButtonRC}" IsEnabled="{Binding IsEnabled}"
                Background="{Binding StringColor}">
                        <Button.Style>
                            <Style TargetType="{x:Type Button}">
                                -->
        <!-- style commun -->
        <!--
                                <Setter Property="Background" Value="Red"/>

                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsReady}" Value="False">
                                        <Setter Property="Content">
                                            <Setter.Value>
                                                <Image Grid.Row="0"
                                            Source="{Binding Source={x:Static draw:SystemIcons.Warning},Converter={StaticResource IconToImageSource},Mode=OneWay}"
                                            HorizontalAlignment="Left" VerticalAlignment="Top" Height="15" Width="15" />
                                            </Setter.Value>
                                        </Setter>
                                        -->
        <!-- style disabled -->
        <!--
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsReady}" Value="True">
                                        <Setter Property="Content" Value="GO"/>
                                        -->
        <!-- style enabled -->
        <!--
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>-->
        <ProgressBar Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Width="250" Height="20" Margin="10,10,10,10"
                     Minimum="0" Maximum="100" IsIndeterminate="True" Visibility="{Binding IsAPICallInPrgress, Converter={StaticResource BooleanToVisibility}}"/>
        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center">

            <Button  Command="{Binding ButtonAddJoueurRC}" Content="Ajouter un joueur" Height="25" Width="150" Margin="15,15,15,15" />
            <Button  Content="Ajouter le(s) jeu(x)" Height="25" Width="150" Margin="15,15,15,15" Visibility="{Binding IsAddJeuVisible, Converter={StaticResource BooleanToVisibility}}" />

        </StackPanel>
    </Grid>
</UserControl>
