<UserControl x:Class="NoNiDev.ArchipelagoParser.Views.RandoStatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NoNiDev.ArchipelagoParser.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
<!-- 0 -->
        <!--<ListBox Grid.Column="0" ItemsSource="{Binding Players}" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <local:SOHARPlayerLine HorizontalContentAlignment="Stretch" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>-->

        <local:APIConnect Grid.Row="0" Grid.Column="1" DataContext="{Binding RandoStatAPIVM}"/>

    </Grid>
</UserControl>
