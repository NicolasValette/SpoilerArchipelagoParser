<UserControl x:Class="NoNiDev.ArchipelagoParser.Views.SOHARPlayerLine"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NoNiDev.ArchipelagoParser.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
           
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <CheckBox Grid.Column="0" IsChecked="{Binding IsChecked}" HorizontalAlignment="Left" Margin="5" Width="50"/>
        <Label Grid.Column="1" Content="{Binding PlayerName}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"/>
        <ComboBox Grid.Column="2" ItemsSource="{Binding PlayerNames}" SelectedItem="{Binding PlayerToSend}" Margin="5" HorizontalAlignment="Right" SelectionChanged="ComboBox_SelectionChanged" Width="75"/>
    </Grid>
</UserControl>

    